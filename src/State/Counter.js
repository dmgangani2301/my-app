import React, { useState } from "react";
import ReactDOM from 'react-dom';
import './style.css';

// const Counter = () => {
//     const [counter,setCounter] = useState(0);
//     const increment = () => {setCounter(setCounter(counter + 1))} //RESET THE COUNTER
//     const decrement = () => {setCounter(setCounter(counter - 1))}

//     return (
//     <div>

        
//         <button onClick={increment}>Increase</button>
//         <button onClick={decrement}>Decrese</button>
//         <button onClick={setCounter}>setCounter</button>
//     </div>
      
//     );
//   };
  
//   export default Counter;

const Counter = () => {
    return (
        <div>
            <h1>counter</h1>
        </div>
    )
}

export default Counter